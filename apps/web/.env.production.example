# Database Configuration for Production
# Ephemeral SQLite database (resets on each deployment)
# This is automatically handled by vercel.json
DATABASE_URL="file:/tmp/deploy.db"

# Basic Authentication (Required)
BASIC_AUTH_USER="admin"
BASIC_AUTH_PASSWORD="change-this-password"

# Google Sheets Integration (Optional)
NEXT_PUBLIC_LOGIC_CHECK_SPREADSHEET_ID=""